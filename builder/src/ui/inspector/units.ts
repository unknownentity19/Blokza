/**
 * Unit lists for measurement controls. Plain data, kept out of the component
 * modules so those can hot-reload.
 */

export const LENGTH_UNITS = ['px', '%', 'rem', 'em', 'vw', 'vh', 'auto'];

export const SIZE_UNITS = ['px', '%', 'rem', 'em', 'vw', 'vh', 'auto', 'fit-content', 'max-content'];

export const SPACING_UNITS = ['px', '%', 'rem', 'em'];

export const MARGIN_UNITS = ['px', '%', 'rem', 'em', 'auto'];

export const FONT_UNITS = ['px', 'rem', 'em', '%'];
